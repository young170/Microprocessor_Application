data   DCD     0x10, 0x20, 0x30, 0x40
data2  DCB     0x10, 0x20, 0x30, 0x40

; storing data:
; storing in words vs bytes.
; results in differently packed memory.
; * DCB must store by 4's to pack word.